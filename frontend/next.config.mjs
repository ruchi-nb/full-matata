/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["source.unsplash.com", "images.unsplash.com", "randomuser.me"], // allow this external image domain
      },
};

export default nextConfig;
